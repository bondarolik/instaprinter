<% title "Indice de promocos" %>

<div id="content" class="row">
  <div class="col-md-12">

  <h2>Listado de promociones</h2>

  <table class="table table-striped">
  <% @banners.each do |bn| %>
    <tr>
      <td width="20%"><strong><%= bn.title %></strong></td>
      <td width="65%"><strong><%= bn.descriptio  %></strong></td>
      <td><%= link_to 'Ver', homebanner_path(bn.id), :class => "btn btn-primary" %></td>
      <td><%= link_to 'Redactar', edit_homebanner_path(bn.id),  :class => "btn btn-warning" %></td>
      <td><%= link_to 'Eliminar', homebanner_path(bn.id), :class => "btn btn-danger", method: :delete, data: { confirm: 'Estas seguro?' } %></td>
    </tr>
  <% end %>
    
  </table>

  <div class="alert alert-info" role="alert">
    <%= link_to 'Agregar nueva promo', new_homebanner_path, :class => "alert-link" %>
  </div>
  </div>
</div>